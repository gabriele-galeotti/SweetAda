
with Definitions;

package Configure is

   pragma Pure;

   use Definitions;

   -- PLATFORM and CPU definitions
   PLATFORM  : constant String := "STM32F769I";
   CPU       : constant String := "@CPU@";
   CPU_MODEL : constant String := "@CPU_MODEL@";

   -- basic configuration parameters
   SYSCLK_FREQUENCY : constant := 200 * MHz1;
   TICK_FREQUENCY   : constant := 1 * kHz1;

end Configure;
