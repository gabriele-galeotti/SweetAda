
#
# Integrator/CP board (QEMU emulator)
#
# ARM926EJ-S LE
#

CPU       := ARM
CPU_MODEL := ARMv5

GCC_SWITCHES_PLATFORM     += -mcpu=arm926ej-s -Wa,-mcpu=arm926ej-s -mabi=aapcs
LD_SWITCHES_PLATFORM      +=
OBJDUMP_SWITCHES_PLATFORM +=
POSTBUILD_ROMFILE         := Y
OBJCOPY_SWITCHES_PLATFORM += --output-target=binary --gap-fill=0x00

USE_APPLICATION := test-IntegratorCP

SESSION_START_COMMAND :=
SESSION_END_COMMAND   :=

RUN_COMMAND   := $(RUNSWEETADA) -k "$(SWEETADA_PATH)" -p "$(PLATFORM_DIRECTORY)" -f qemu.cfg
DEBUG_COMMAND := $(RUN_COMMAND) -d DEBUG_QEMU

