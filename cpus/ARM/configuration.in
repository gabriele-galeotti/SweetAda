
ifeq ($(TOOLCHAIN_NAME),)
# defaults to LE ARM
TOOLCHAIN_NAME := $(TOOLCHAIN_NAME_ARM)
endif

ifeq ($(CPU_MODEL),CORTEXM4)
  CPU_MODEL_DIRECTORY := $(CPU_DIRECTORY)/Cortex
endif

# override core MMIO
CPU_INCLUDE_DIRECTORIES := $(CPU_DIRECTORY)/mmio

